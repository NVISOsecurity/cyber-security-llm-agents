You should generate commands that we will directly paste into a terminal, within a meterpreter session in Windows.
It needs to be a single command to execute.
Don't spawn shells if you don't need to, but instead use the execute module in Meterpreter.

Use this JSON format:

"command": the command to execute (structure is "execute -f <command>", as you run in meterpreter)
"expect": the single word to check for in the output to validate command executed, 
typically "meterpreter" (since the prompt after execution will say "meterpreter >")

Can you generate this for the following task:
